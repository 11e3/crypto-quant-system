# Test configuration for unit tests
# This file is used for testing and should not contain real API keys

trading:
  tickers:
    - KRW-BTC
    - KRW-ETH
    - KRW-XRP
  max_slots: 4
  min_order_amount: 5000.0
  fee_rate: 0.0005
  slippage_rate: 0.0005

strategy:
  name: "VanillaVBO"
  sma_period: 4
  trend_sma_period: 8
  short_noise_period: 4
  long_noise_period: 8

bot:
  daily_reset_hour: 9
  daily_reset_minute: 0
  api_retry_delay: 1
  websocket_reconnect_delay: 5

telegram:
  enabled: false  # Disabled for tests
  token: "test_token"
  chat_id: "test_chat_id"

upbit:
  access_key: "test_access_key"
  secret_key: "test_secret_key"
