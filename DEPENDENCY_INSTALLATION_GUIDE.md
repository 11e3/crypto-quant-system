# Quick Start - 의존성 설치 가이드

## 📦 설치 옵션

### 프로덕션 환경 (실행 봇)
```bash
# 최소 의존성만 설치
pip install -e .
```

**포함된 패키지:**
- 거래소 연동 (pyupbit)
- 데이터 처리 (pandas, numpy)
- 설정 관리 (pydantic, pyyaml)
- 실행 도구 (click, psutil)

**이미지 크기:** ~300MB
**설치 시간:** ~2분

---

### 개발 환경 (분석 + 테스트)
```bash
# 모든 기능 포함
pip install -e ".[analysis,dev,test,notebooks]"
```

**추가 포함:**
- 분석 도구 (scipy, matplotlib, seaborn)
- 테스트 도구 (pytest, mypy, ruff)
- 노트북 (jupyter)

**이미지 크기:** ~450MB
**설치 시간:** ~4분

---

### 백테스팅/분석만
```bash
# 프로덕션 + 분석 도구
pip install -e ".[analysis]"
```

**사용 케이스:**
- 전략 백테스팅
- 포트폴리오 최적화
- 성능 분석 및 리포트 생성

---

## 🐳 Docker 최적화

### 멀티 스테이지 빌드 권장

```dockerfile
# Stage 1: 프로덕션 이미지 (최소)
FROM python:3.14-slim AS base
WORKDIR /app
COPY . .
RUN pip install -e .
CMD ["crypto-quant", "run-bot"]

# Stage 2: 분석 이미지 (확장)
FROM base AS analysis
RUN pip install -e ".[analysis]"
CMD ["crypto-quant", "backtest"]
```

**효과:**
- 프로덕션 이미지: ~150MB 절감
- 빌드 시간: ~40% 단축
- 레이어 캐싱 효율 증가

---

## 🔧 의존성 그룹 상세

### `analysis` - 분석 및 최적화
```toml
scipy>=1.10.0      # 포트폴리오 최적화, 통계 검정
matplotlib>=3.7.0  # 플롯팅
seaborn>=0.12.0    # 통계 시각화
```

**사용처:**
- 백테스팅 리포트 생성
- 포트폴리오 최적화 (MPT, Risk Parity)
- 통계적 검증 (Bootstrap, Permutation Test)

### `dev` - 개발 도구
```toml
pytest>=7.4.0      # 테스트 프레임워크
mypy>=1.5.0        # 타입 체커
ruff>=0.1.0        # 린터/포맷터
pre-commit>=3.4.0  # Git 훅
```

### `test` - 테스트 전용
```toml
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
```

### `notebooks` - Jupyter 환경
```toml
jupyter>=1.0.0
ipykernel>=6.25.0
ipywidgets>=8.1.0
```

---

## 📊 성능 비교

| 환경 | 패키지 수 | 이미지 크기 | 설치 시간 |
|------|----------|------------|----------|
| 프로덕션 | ~15 | 300MB | 2분 |
| 프로덕션+분석 | ~25 | 450MB | 3.5분 |
| 풀 개발 | ~45 | 600MB | 5분 |

---

## ⚠️ 주의사항

### numpy/scipy 버전 호환성
- `numpy>=1.24.0` + `scipy>=1.10.0` 조합 권장
- 이전 버전 사용 시 호환성 문제 가능

### Windows 환경
- Visual C++ Redistributable 필요 (scipy 컴파일)
- 또는 pre-built wheel 사용

### M1/M2 Mac
```bash
# Rosetta 없이 native 설치
pip install --no-binary :all: --compile scipy
```

---

## 🔄 마이그레이션 (기존 사용자)

### 이전 설치 방식
```bash
# 모든 의존성이 자동 설치됨
pip install -e .
```

### 새로운 설치 방식
```bash
# 프로덕션 (봇 실행만)
pip install -e .

# 백테스팅 필요 시
pip install -e ".[analysis]"

# 개발 시
pip install -e ".[analysis,dev,test]"
```

**영향:**
- 기존 CI/CD 스크립트 업데이트 필요
- Dockerfile 수정 권장 (위 참조)
- 로컬 개발 환경 재설치 필요

---

## 📝 체크리스트

프로덕션 배포 전:
- [ ] 최소 의존성으로 테스트 (`pip install -e .`)
- [ ] Docker 멀티 스테이지 빌드 적용
- [ ] CI/CD 파이프라인 업데이트
- [ ] 이미지 크기 확인 (300MB 이하 권장)

개발 환경 설정:
- [ ] 모든 그룹 설치 (`pip install -e ".[analysis,dev,test]"`)
- [ ] pre-commit 훅 설치 (`pre-commit install`)
- [ ] 테스트 실행 확인 (`nox -s tests-3.14`)
- [ ] 타입 체크 통과 (`nox -s type_check`)
